# Optional: live tutoring via xAI (server-side only). Without a key, offline educational replies still work.
XAI_API_KEY=

# Optional alternate name some environments use:
# GROK_API_KEY=

# Optional database URL for auth/progress if you enable server persistence:
# DATABASE_URL=
